# Python environment (rebuilt from requirements.txt)
.venv/
__pycache__/

# Raw downloads (extract.py can download them again at any time)
data/raw/
# Clean data (clean.py rebuilds it from the raw file in seconds)
data/clean/

# The warehouse (load.py rebuilds it from the clean file in seconds)
data/*.duckdb
